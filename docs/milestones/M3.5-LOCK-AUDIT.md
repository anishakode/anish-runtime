## Lock audit: through M3.5

**Trigger:** Owner locked M3.5 (“Okay lets go next locked”)  
**Stages re-checked:** M0 · M1 · M2 · M3 · M3.5  
**Generated:** 2026-09-14

### CRITICAL

_None._

### HIGH

_None._

### MEDIUM

1. **Local `pnpm ci` omits Playwright e2e** — GitHub Actions still runs `test:e2e`. Run `pnpm test:e2e` when verifying browser recruiter UX and `/evidence.json` smoke.
2. **Boundary honesty nodes** remain graph-only (no dedicated UI) — by design until later museum/Fork work.

### LOW / deferred

- RUN ANISH / landing compilation → **M4**
- Labs / Signal / Fork / Ending Signal → later milestones
- Dark-default theme out of Editorial Lab light-first contract
- Heavy cross-OS screenshot VR deferred
- CSP `unsafe-inline` / `unsafe-eval` → **M24**

### Solid

**M0** — Scope validator, CI baseline, no premature WOW paths, security headers intact.  
**M1** — Evidence graph validates (10 / 38 / 41 / 21); exclusions; weak states honest; SHA fingerprints required.  
**M2** — Recruiter routes + nav; 404; `/llms.txt`; pinned repo links; print chrome scoped.  
**M3** — Editorial Lab tokens/fonts; legend; forced-colors / reduced-motion / print / focus-visible; TOKEN_HEX sync + AA contrast; Active Instrument hooks only.  
**M3.5** — `/evidence.json` derived projection (`anish-runtime.evidence.public` v1); authority headers; fingerprints; exclusion leak guards; `validate:evidence` covers graph + manifest; `/llms.txt` discovery; unit + e2e coverage.

### Commands run

- `pnpm validate:scope` → OK
- `pnpm validate:evidence` → OK (graph + Public evidence manifest)
- `pnpm test:status` → **50 passed / 0 failed**
- `pnpm format:check` → OK
- `pnpm lint` → OK
- `pnpm typecheck` → OK
- `pnpm build` → OK (`/evidence.json` in route table)

**Lock hygiene:** Clean

**Next:** Propose M4 — Landing Compilation + Home Runtime (await owner approval — do not implement until Approve M4).
