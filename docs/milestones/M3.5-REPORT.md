## Milestone report: M3.5 — Public Evidence Manifest

**Scope approved:** Owner — approve if no changes required (proposal accepted as-is)

**M3 status:** Locked

**Implemented:**

- `buildPublicEvidenceManifest` + Zod public schema (`src/lib/evidence/public-manifest.ts`)
- Static route `/evidence.json` with derived/projection headers
- Source SHA-256 fingerprints; GitHub SHA retained
- Exclusion leak guards (obsolete email / sricons)
- `/llms.txt` discovers `/evidence.json` + authority note
- `pnpm validate:evidence` validates graph **and** public manifest
- ADR 0005

**Tests added/updated:**

- `public-manifest.test.ts` — schema, scale, fingerprints, exclusion hygiene, fail-closed poison test
- E2e request smoke for `/evidence.json` + llms discovery line

**Test status:** **50 passed / 0 failed** — see `docs/testing/TEST-STATUS.md`

**Validation run:** scope · evidence (graph + manifest) · test:status · format · lint · typecheck · build — OK

**Truth / privacy notes:** Repo graph authoritative; JSON derived; exclusions never projected

**Known gaps / deferred:** M4 RUN ANISH / labs / Signal; CSP (M24)

**Status:** Locked (owner — 2026-09-14)

**Lock audit:** `docs/milestones/M3.5-LOCK-AUDIT.md`
