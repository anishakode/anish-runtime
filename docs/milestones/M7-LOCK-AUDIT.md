## Lock audit: through M7

**Trigger:** Owner — lock M6 path and ship signature uniqueness (“Yes please make sure we are doing a great unique work”)  
**Stages re-checked:** M0 · M1 · M2 · M3 · M3.5 · M4 · M5 · M6 · M7  
**Generated:** 2026-09-15

### CRITICAL

_None._

### HIGH

_None._

### MEDIUM

1. **Local `pnpm ci` omits Playwright e2e** — GHA still runs it.
2. **Boundary `NOT_DEMONSTRATED` nodes** still graph/legend/JSON only.

### LOW / deferred

- Dedicated Source Trace drawer UI → **M8**
- Steward / Malware labs, Signal, Fork → later
- CSP hardening → M24

### Solid

**M0–M4** — foundation, evidence, recruiter path, visual system, public manifest, RUN ANISH.  
**M5** — deterministic PSI/KS core aligned to pinned sources.  
**M6** — `/labs/mlops` inspectable lab (SHIFT / INJECT / RESET, table, disagreement, honesty).  
**M7** — **BREAK THE SYSTEM** + Watch Anish Debug + labeled event trace + RECOVER; simulated notify only; home link into lab.

### Commands run

- `pnpm validate:scope` → OK
- `pnpm validate:evidence` → OK
- `pnpm test:status` → **105 passed / 0 failed**
- `pnpm format:check` · lint · typecheck · build → OK

**Lock hygiene:** Clean

**Next:** Propose M8 — Source Trace Mode (await Approve M8).
